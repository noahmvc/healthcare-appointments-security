# healthcare-appointments-security

Small healthcare appointment web app for the SSDF PO.3 toolchain setup assignment.

The app is intentionally simple.

It lets a user submit a mock healthcare appointment request.

It does not store real patient data.

It is used to demonstrate secure development tooling such as CodeQL, dependency checks, and GitHub workflow output.

## Run locally

```bash
npm test
npm start
